# Integration tests

Run these tests against GenLayer Studio or a configured GenLayer network after
installing the official GenLayer testing dependencies. The consensus path must
be exercised in a real GenLayer environment because it uses nondeterministic web
access and the Equivalence Principle.
