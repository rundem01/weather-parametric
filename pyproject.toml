[project]
name = "weather-parametric-insurance"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "cryptography>=42,<47",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
